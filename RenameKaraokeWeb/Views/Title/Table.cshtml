@model  RenameKaraokeWeb.Models.SongRecord

@{
    ViewBag.Title = "Table";
}
<div class="spinner-border text-primary" role="status">
    <span class="sr-only">Loading...</span>
</div>
<table class="table table-striped">
    <caption>Master Song List</caption>
    <tr class="template-row">
        <th class="" title="ID">
            <span>ID</span>
        </th>
        <th class="" title="Song Title">
            <span>Song Title</span>
        </th>
        <th class="" title="Artist">
            <span>Artist</span>
        </th>
    </tr>
    @{ 
        int i = 0;
        foreach (var songRecord in Model.SongRecords)
        {
            //Every Other Entry is an Entry That Does not need to be used 
            if (i % 2 == 0)
            {
            <tr>
                <td class=" iD">
                    @songRecord.ID
                    @*@Html.DisplayFor(s => s.SongRecords[i].ID, new { @id = "Id" + i })*@
                </td>
                <td class="song-Title">
                    @songRecord.Title
                    @*@Html.DisplayFor(s => s.SongRecords[i].Title, new { @id = "Song Title" + i })*@
                </td>
                <td class="artist">
                    @songRecord.Artist
                    @*@Html.DisplayFor(s => s.SongRecords[i].Artist, new { @id = "Artist" + i })*@
                </td>
            </tr>
                }
                i++;
            }
        }

</table>

